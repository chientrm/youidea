{
  "name": "local-worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler publish",
    "start": "wrangler dev",
    "db": "npx wrangler d1 execute D1 --local --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230419.0",
    "cf-workers-proxy": "file:../../cf-workers-proxy",
    "typescript": "^5.0.4",
    "wrangler": "^3.0.0"
  }
}